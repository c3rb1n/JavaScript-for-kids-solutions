// Make object
let scores = {
	artyom: 0,
	viktor: 15,
	egor: 3,
	artur: 10,
	boris: 5
};

// Increase Artyom's scores by 10
scores.artyom += 10;

// Increase Viktor's scores by 20
scores.viktor += 20;

// Increase Egor's scores by 30
scores.egor += 30;

// Increase Artur's scores by 40
scores.artur += 40;

// Increase Boris's scores by 50
scores.boris += 50;

// Show scores
scores; // {artyom: 10, viktor: 35, egor: 33, artur: 50, boris: 55}